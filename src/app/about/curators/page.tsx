'use client';
import Header from '@/components/Header';
import { motion } from 'framer-motion';
import { CheckCircle2, Leaf, Loader2, UserRound } from 'lucide-react';
import { useEffect, useState } from 'react';
import { useLanguage } from '@/context/LanguageContext';
import { supabase } from '@/lib/supabase';
type Curator = { id: string; username: string | null; avatar_url: string | null; role: string };
export default function CuratorsPage() {
  const { language } = useLanguage(); const isZh = language === 'zh';
  const [curators, setCurators] = useState<Curator[]>([]); const [loading, setLoading] = useState(true);
  useEffect(() => { let active = true; const load = async () => { const result = await supabase.from('profiles').select('id, username, avatar_url, role').eq('role', 'curator').eq('status', 'active').order('username', { ascending: true }); if (active) { if (result.error) console.error('Unable to load curator list:', result.error); setCurators((result.data as Curator[] | null) ?? []); setLoading(false); } }; load(); return () => { active = false; }; }, []);
  const teamIntro = isZh ? [{ title: '管理團隊', text: '負責平台的整體方向、資料品質及跨分類群協作。管理團隊確保資料庫的內容、功能和社群工作保持一致，並持續推動香港自然資料的開放與共享。' }, { title: '社群策展人', text: '由具備自然觀察及研究經驗的成員組成，協助整理、核實及補充資料。他們會參考可靠來源，回應社群建議，並尊重每一位觀察者和資料貢獻者。' }] : [{ title: 'Management team', text: 'Guides the platform, data quality and collaboration across taxonomic groups. The management team keeps our content, features and community work aligned while advancing open access to Hong Kong nature data.' }, { title: 'Community curators', text: 'Members with natural history and research experience who help review and enrich records. They consult reliable sources, respond to community suggestions and respect every observer and contributor.' }];
  return <div className="min-h-screen bg-slate-50 text-slate-900"><Header /><main className="mx-auto max-w-6xl px-6 pb-20 pt-36 md:px-10"><motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }}><p className="mb-4 text-xs font-black uppercase tracking-[0.28em] text-emerald-600">People behind the records</p><h1 className="mb-5 text-4xl font-black md:text-6xl">{isZh ? '策展人' : 'Curators'}</h1><p className="max-w-3xl text-lg leading-relaxed text-slate-600">{isZh ? '策展人守護資料的準確性、脈絡和可用性，讓每一筆自然紀錄都能成為值得信賴的公共知識。' : 'Curators protect the accuracy, context and usability of our records, helping every natural history entry become trusted public knowledge.'}</p></motion.div><section className="mt-12 rounded-[2rem] bg-white p-8 shadow-xl shadow-slate-200/40 md:p-12"><div className="grid gap-4 border-b border-slate-100 pb-10 md:grid-cols-3"><div><Leaf className="mb-4 h-8 w-8 text-emerald-600" /><h2 className="text-2xl font-black">{isZh ? '策展人的責任' : 'What curators do'}</h2></div><p className="leading-8 text-slate-600 md:col-span-2">{isZh ? '策展人會協助檢視物種名稱、分類、照片、分布和保育資料；為內容補充可靠來源和註解；處理社群提交的修訂建議；並與其他專長人士合作，持續改善資料庫的完整性。' : 'Curators review names, taxonomy, photographs, distribution and conservation information; add sources and context; respond to community suggestions; and work with specialists to improve the database.'}</p></div><div className="mt-10"><h2 className="mb-5 text-2xl font-black">{isZh ? '團隊簡介' : 'Our teams'}</h2><div className="mb-12 grid gap-5 md:grid-cols-2">{teamIntro.map((team) => <article key={team.title} className="rounded-3xl border border-slate-100 bg-slate-50 p-7"><h3 className="mb-3 text-xl font-black">{team.title}</h3><p className="leading-relaxed text-slate-600">{team.text}</p></article>)}</div><h2 className="mb-5 text-2xl font-black">{isZh ? '現任策展人列表' : 'Current curator directory'}</h2>{loading ? <div className="flex items-center gap-2 py-8 text-sm font-bold text-slate-400"><Loader2 className="h-4 w-4 animate-spin" />{isZh ? '載入策展人資料…' : 'Loading curators…'}</div> : curators.length === 0 ? <div className="rounded-2xl border border-dashed border-slate-200 bg-slate-50 p-8 text-center text-slate-500">{isZh ? '目前未有公開的策展人資料。' : 'No public curator profiles yet.'}</div> : <div className="grid gap-5 md:grid-cols-2">{curators.map((curator) => <article key={curator.id} className="flex items-center gap-5 rounded-3xl border border-slate-100 bg-slate-50 p-6"><div className="flex h-16 w-16 shrink-0 items-center justify-center overflow-hidden rounded-2xl bg-emerald-100 text-emerald-700">{curator.avatar_url ? <img src={curator.avatar_url} alt={curator.username || 'Curator'} className="h-full w-full object-cover" /> : <UserRound className="h-7 w-7" />}</div><div><div className="mb-1 flex items-center gap-2"><h3 className="text-lg font-black">{curator.username || (isZh ? '未命名策展人' : 'Unnamed curator')}</h3><CheckCircle2 className="h-4 w-4 text-emerald-600" /></div><p className="text-sm font-bold text-emerald-700">{isZh ? '策展人' : 'Curator'}</p></div></article>)}</div>}</div></section></main></div>;
}
